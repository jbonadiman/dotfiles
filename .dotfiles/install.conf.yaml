- defaults:
    link:
      relink: true

- clean: ['~']

#- link:
#    ~/.tmux.conf: tmux.conf
#    ~/.vim: vim
#    ~/.vimrc: vimrc

- create:
  - ~/sources

# - shell:
#   - [git submodule update --init --recursive, Installing submodules]
