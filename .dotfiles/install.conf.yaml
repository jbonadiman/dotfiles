- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.vim: vim
    ~/.vimrc: vimrc
    ~/.zshrc: zshrc

- create:
  - ~/sources

# - shell:
#   - [git submodule update --init --recursive, Installing submodules]
