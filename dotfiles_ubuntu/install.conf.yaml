- defaults:
    link:
      relink: true

- clean: ['~']

- create:
  - ~/sources
  - ~/.local/bin

- link:
    ~/.vimrc:
    ~/.zshrc:
    ~/.zshenv:
    ~/.config/bat:
      create: true
      path: config/bat

- shell:
  -
    command : ./essential_packages.sh
    stdout: true
    stderr: true
