- defaults:
    link:
      relink: true

- clean: ['~']

- create:
  - ~/sources
  - ~/.local/bin

- link:
    ~/.vimrc:
    ~/.zshrc:
    ~/.config/bat:
      create: true
      path: config/bat

- shell:
  -
    command : ./essential_packages.sh
    stdout: true
    stderr: true
