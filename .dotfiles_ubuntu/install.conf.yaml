- defaults:
    link:
      relink: true

- clean: ['~']

- create:
  - ~/sources
  - ~/.local/bin

- link:
    ~/.vim:
    ~/.vimrc:
    ~/.zshrc:
    ~/.local/bin/bat: /usr/bin/batcat

- shell:
#   - [git submodule update --init --recursive, Installing submodules]
