- defaults:
    link:
      relink: true

- clean: ['~']

- create:
  - ~/sources
  - ~/.local/bin

- link:
    ~/.vim:
    ~/.vimrc:
    ~/.zshrc:
    ~/.local/bin/bat: /usr/bin/batcat

- shell:
  -
    command : ./essential_packages.sh
    stdout: true
    stderr: true
